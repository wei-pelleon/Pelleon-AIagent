version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd ux
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: ux/dist
    files:
      - '**/*'
  cache:
    paths:
      - ux/node_modules/**/*
